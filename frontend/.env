VITE_API_BASE_URL="http://localhost:3003/api"

# Fallback redoslijed (comma-separated). Ako je postavljeno, ima prioritet nad VITE_API_BASE_URL.
# Primarno za Docker okruženja preporuka je koristiti relativni URL: "/backend/api"
# i proslijediti ga kao build arg / env var u compose-u.
